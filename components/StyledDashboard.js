import styled from 'styled-components';

const StyledDashboard = styled.div`
  min-height: 100vh;
  padding: 35px 35px 35px calc(450px + 35px);
  background-color: #fff;
`;

export default StyledDashboard;
